package com.github.mnemotechnician.mkui

import kotlin.test.*
import arc.scene.ui.*
import arc.scene.ui.layout.*

class TableTest {
	
	/*
	//TODO: doesn't work because arc core is not initialized
	
	var testTable = Table()
	var el1 = TextButton("amogus", mindustry.ui.Styles.defaultt)
	var el2 = Table()
	
	init {
		testTable.add(el1)
		testTable.add(el2)
	}

	@Test fun `test whether child methods return correct values`() {
		assertEquals(testTable.child(0), el1, "wrong child returned by child()!")
		assertEquals(testTable.childOrNull(1), el2, "wrong child returned by childOrNull()!")
		
		assertNull(testTable.childOrNull(6), "non-null value returned by childOrNull()!")
		assertNull(testTable.childAsOrNull<ImageButton>(1), "non-null value returned by childAsOrNull()!")
		
		assertEquals(testTable.childAsOrNull<TextButton>(0)?.text, "amogus", "idk how to describe this")
	}
	*/
	
}
